# cuda-hpc

CUDA / GPU high-performance computing projects.

## Projects

- [`01_parallel_ode_integrator`](01_parallel_ode_integrator/) — solve a batch of ODEs in parallel on the GPU, benchmarked against a CPU baseline.
- [`02_nbody_simulation`](02_nbody_simulation/) — all-pairs gravitational N-body on the GPU (one thread per body) vs a CPU baseline.
