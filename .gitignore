.vscode/
build/
*.o
*.exe
*.exp
*.lib
*.pdb
benchmark

# stray benchmark artifacts if the program is ever run from the repo root
# (the real ones live in cuda-hpc/01_parallel_ode_integrator/)
/benchmark.png
/results.csv
